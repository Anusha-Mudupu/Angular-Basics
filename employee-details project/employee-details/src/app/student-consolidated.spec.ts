import { StudentConsolidated } from './student-consolidated';

describe('StudentConsolidated', () => {
  it('should create an instance', () => {
    expect(new StudentConsolidated()).toBeTruthy();
  });
});
